[Unit]
Description=Remote desktop service (VNC)

[Service]
Type=simple
# No security needed as it can only be accessed via local host on port 5900
ExecStart=/usr/bin/x0vncserver -localhost -SecurityTypes none

[Install]
WantedBy=default.target